import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    paddingTop: 8,
    paddingHorizontal: 18,

    flex: 1,
  },

  resultsContainer: {
    flex: 1,
    gap: 14,
  },

  loading: {
    width: 130,
    height: 130,

    alignSelf: 'center',
  },
});
